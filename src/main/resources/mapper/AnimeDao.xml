<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.unual.anime.dao.AnimeDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->
    <select id="queryById" resultType="Anime" parameterType="int">
        <!-- 具体的sql -->
        SELECT
        anime_id,
        anime_name,
        anime_url,
        anime_img
        FROM
        anime_list
        WHERE
        anime_id = #{animeId}
    </select>

    <select id="queryAll" resultType="Anime">
        SELECT
            anime_id,
            anime_name,
            anime_url,
            anime_img
        FROM
            anime_list
        WHERE
            anime_name
            LIKE ${filter}
        ORDER BY
            anime_id
        LIMIT ${page*limit}, ${(page+1)*limit}
    </select>

    <select id="queryTVAll" resultType="Anime">
        SELECT
            anime_id,
            anime_name,
            anime_url,
            anime_img
        FROM
            tv_list
        WHERE
            anime_name
            LIKE ${filter}
        ORDER BY
            anime_id
        LIMIT ${page*limit}, ${(page+1)*limit}
    </select>
</mapper>